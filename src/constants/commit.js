// @flow
const COMMIT_MODES = Object.freeze({
  CLIENT: 'client',
  HOOK: 'hook'
})

export const COMMIT_MESSAGE_PARTS = Object.freeze({
  MESSAGE: 'message',
  SCOPE: 'scope',
  TITLE: 'title'
})

export default COMMIT_MODES
